#include <stdio.h>
#include <stdlib.h>

int main(){
  int a; // начальное значение
  int n; // количество шагов
  int k;
  int s = 1; // итоговая сумма
  // считать a, n
  scanf("%d",&a);
  scanf("%d",&n);
  // повторять:
  // s = s * (a+k)
  // k = k + 1
  // до тех пор, пока k = n
  // вывести s
  return 0;
}
